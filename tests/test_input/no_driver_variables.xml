<?xml version="1.0" encoding="UTF-8"?>
<dynamical-system>

  <!-- Every system must define drivers including a timestep attribute
       and with at least one row having at least one variable. -->

  <drivers timestep="1">
    <row>
      <!-- But the row in this system's drivers has no variables. -->
    </row>
  </drivers>
</dynamical-system>
